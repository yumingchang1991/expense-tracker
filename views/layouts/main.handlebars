<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" >
  <title>{{#if isAuthenticated}}{{user.displayName}}的{{/if}}家庭記帳本</title>
</head>
<body>
  <nav class="navbar navbar-light bg-light">
    <div class="container d-flex justify-content-space-between">
      <a class="navbar-brand" href="/">
        {{#if isAuthenticated}}{{user.displayName}}的{{/if}}家庭記帳本
      </a>
      {{#if isAuthenticated}}
      <div class="p-0">
        <a href="/users/edit" class="btn btn-success">修改會員資料</a>
        <a href="/users/logout" class="btn btn-secondary">登出</a>
      </div>
      {{else}}
      <a href="/users/login" class="btn btn-success">登入</a>
      {{/if}}
    </div>
  </nav>
  {{{body}}}
</body>
</html>